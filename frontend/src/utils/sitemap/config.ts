import type { SitemapConfig } from './types';

export const sitemapConfig: SitemapConfig = {
  baseUrl: 'https://foundationrepairadelaide.com.au',
  defaultChangeFreq: 'monthly',
  defaultPriority: 0.5
};